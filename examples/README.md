# Examples

これらは explore-engine の**出力サンプル**であり、AIエージェントが生成したリサーチ結果です。

- 事実検証は通しておらず、引用元の信頼性も玉石混淆です（業界一次ソース・コンテンツファーム・SEOブログが混在）
- 製品名・モデル名・日付の中にエージェントの誤りや幻覚が含まれる可能性があります
- 「**フォーマットの実例**」として参照してください

実運用では、出力をそのまま信じず、`出典:` のURLを各自で確認してください。
